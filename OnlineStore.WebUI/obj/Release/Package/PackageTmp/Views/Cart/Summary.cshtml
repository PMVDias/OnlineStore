@model OnlineStore.Domain.Entities.Cart

<div class="navbar-right hidden-xs" style="margin-top: 5px;">
    <a href=@Url.Action("Index", "Cart", new { returnUrl = Request.Url.PathAndQuery }) class="btn btn-default navbar-btn">
        <span class="glyphicon glyphicon-shopping-cart"><span style="margin-left:5px;font-family:sans-serif;font-size: 14px;font-weight: 300;">Cart<span style="margin-left:5px">(@Model.Lines.Sum(x => x.Quantity))</span></span></span>
    </a>

</div>
<div class="navbar-right visible-xs" style="margin-top: -35px;">
    <a href=@Url.Action("Index", "Cart", new { returnUrl = Request.Url.PathAndQuery }) class="btn btn-default navbar-btn">
        <span class="glyphicon glyphicon-shopping-cart"></span>
    </a>
</div>
@*<div class="navbar-text navbar-right">
        <b class="hidden-xs">Your cart:</b>
        @Model.Lines.Sum(x => x.Quantity) item(s),
        @Model.ComputeTotalValue().ToString("c")
    </div>*@
